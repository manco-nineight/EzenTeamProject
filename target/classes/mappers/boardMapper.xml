<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.board">



	<delete id="deleteAllFilenameByBno">
		DELETE FROM attach WHERE bno = #{bno}
	</delete>

	<delete id="deleteFilename">
		DELETE FROM attach WHERE filename = #{filename}
	</delete>

	<select id="getAttach" resultType="string">
		SELECT * FROM attach WHERE bno = #{bno}
	</select>

	<insert id="addAttach">
		INSERT INTO attach (filename, bno) VALUES (#{filename}, #{bno})
	</insert>

	<update id="increaseReadcnt">
		UPDATE board SET readcnt = readcnt + 1 WHERE bno = #{bno}
	</update>


   <update id="readcnt">

UPDATE board SET readcnt = readcnt+1 WHERE bno=#{bno}

</update>

   <insert id="insert">
		INSERT INTO board (bno, title, content, writer) VALUES
		((SELECT NVL2(MAX(bno), MAX(bno)+1, 1) FROM board), 
			#{title}, #{content}, #{writer})
			
			
			<selectKey order="AFTER" keyProperty="bno" resultType="Integer">
				SELECT MAX(bno) FROM board
			</selectKey>
			
			
	</insert>

	<delete id="delete">
		DELETE FROM board WHERE bno=#{bno}
	</delete>


	<update id="update">

		UPDATE board SET writer = #{writer}, title = #{title}, content = #{content},
		updatedate = SYSDATE
		WHERE bno=#{bno}

	</update>
	
	<select id="content" resultType="kr.co.domain.BoardVO">
		SELECT * FROM board WHERE content like #{put} ORDER BY bno DESC
	</select>
	
	
	<select id="title" resultType="kr.co.domain.BoardVO">
		SELECT * FROM board WHERE title like #{put} ORDER BY bno DESC
	</select>
	


	<select id="writer" resultType="kr.co.domain.BoardVO">
		SELECT * FROM board WHERE writer like #{put} ORDER BY bno DESC
	</select>
	
	<select id="getContentAmount" resultType="integer">
      SELECT COUNT(bno) FROM board WHERE content like #{put}
   </select>
   
   <select id="getTitleAmount" resultType="integer">
      SELECT COUNT(bno) FROM board WHERE title like #{put}
   </select>
	
	<select id="getSAmount" resultType="integer">
      SELECT COUNT(bno) FROM board WHERE writer like #{put}
   </select>

  <select id="getAmount" resultType="integer">
      SELECT COUNT(bno) FROM board
   </select>

   
   
   <select id="list" resultType="kr.co.domain.BoardVO">
   
   SELECT * FROM board ORDER BY bno DESC
   
   </select>

	<select id="read" resultType="kr.co.domain.BoardVO">
		SELECT * FROM board WHERE bno = #{bno}
	</select>
	
	
	

</mapper>